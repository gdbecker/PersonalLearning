## 02_ Docker Images and Containers: The Core Building Blocks

### Commands: Using and Running External (Pre-Built) Images
- Run "docker run node"
  - Uses the Node.js image already available from Docker Hub